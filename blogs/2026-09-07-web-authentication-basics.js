window.BLOG_REGISTRY = window.BLOG_REGISTRY || [];
window.BLOG_REGISTRY.push({
  "id": "2026-09-07-web-authentication-basic-bearer-tokens-and-jwt",
  "title": "Web Authentication: Basic, Bearer Tokens and JWT",
  "author": "Sukee Parker",
  "date": "2026-09-07",
  "category": "Technology",
  "tags": [
    "Security"
  ],
  "excerpt": "Basic authentication, including username/password, tokens, and JWT.",
  "content": "<p><b>1. Basic Authentication: Send Your Credentials for Every Web Request</b><br></p><p>Basic Authentication is straightforward:&nbsp;Username + Password → Base64 encoding → HTTP Authorization Header → Server.&nbsp;The client combines the username and password, Base64-encodes them, and sends the resulting value with each&nbsp;<span>API authentication&nbsp;</span><span>request. Because&nbsp;</span><b>Base64 is not encryption</b><span>, does not make that information secret.&nbsp;That is why </span><b>HTTPS is essential </b>to&nbsp;<span>encrypts the connection between the client and server.</span></p><p><b>2. Bearer Tokens: Authenticate Once, Use a Token Afterward</b></p><p>Bearer tokens improve this model.&nbsp;Instead of repeatedly sending your password, you authenticate once:&nbsp;<span>Username + Password → Authentication Server → Bearer Token.&nbsp;</span><span>Subsequent requests look more like:&nbsp;Bearer Token → API → Response. The token is like the wristband for</span><span>&nbsp;a music festival. &nbsp;You show your ticket once at the entrance and receive a wristband. For the rest of the event, you show the wristband rather than repeatedly presenting the original ticket. &nbsp;</span><span>A traditional bearer token is </span><b>O</b><b>paque Bearer Tokens</b><span> which&nbsp;is simply a random string.&nbsp;For example:&nbsp;8f4a92c71b...&nbsp;The token itself tells the API essentially nothing but it can acts like a coat-check ticket: the number is meaningful because the system can look it up. The opaque tokens are easy to control and revoke, but requires shared infrastructure to looking up for user information, which hinder the application grows.&nbsp;</span></p><p><b>3. JWT: Put the Information Inside the Token</b></p><p>A&nbsp;JSON Web Token (JWT)&nbsp;is a self-contained token.&nbsp;Instead of giving the server an arbitrary identifier that must always be looked up, the token can <b>carry claims about the authenticated identity.</b>&nbsp;A JWT contains three sections:&nbsp;Header.Payload.Signature.&nbsp;The&nbsp;header&nbsp;describes information about the token, including the signing algorithm and token type.&nbsp;The payload contains&nbsp;claims.&nbsp;These can include information such as:Subject,User ID,&nbsp;Role,&nbsp;Permissions,&nbsp;Issued-at time and&nbsp;Expiration time.&nbsp;But there is a crucial security rule:&nbsp;Do not put secrets in a normal JWT payload.&nbsp;The&nbsp;payload&nbsp;is encoded, not inherently encrypted. Someone possessing the token can decode its payload. Passwords, credit-card information, and other sensitive secrets therefore do not belong there.&nbsp;The&nbsp;signature&nbsp;is what allows the receiver to determine whether the signed contents have been modified.</p><p><span>Conceptually:&nbsp;</span><span>Header + Payload + Signing Key → Cryptographic Signature</span><br></p><p>Now, there are two popular&nbsp;<span>Cryptographic Signature algorithm:&nbsp;</span></p><p><span>HS256 is theSymetric Signing, where The same secret is used to sign and verify tokens. This is relatively straightforward when one trusted system controls both operations.</span></p><p>RS256 is Asymmetric Signing.&nbsp;Two different keys are used:&nbsp;Private Key → Sign.&nbsp;Public Key → Verify.&nbsp;<span>The authentication service keeps the private key secret. Other services only need the public key to verify tokens.&nbsp;This is especially useful in a distributed architecture because downstream services can verify authentication tokens without receiving the private signing key.</span></p><p><span>JWT Became Popular in Distributed Systems because it&nbsp;changes an important architectural assumption. &nbsp;</span><span>The server can often verify the token cryptographically without querying a centralized token database for every request.&nbsp;This becomes particularly useful when an application contains many services, where client use JWT to &nbsp;access services through API gateway.&nbsp;Each service can potentially verify the token independently.&nbsp;That makes JWT attractive for distributed architectures where stateless verification and horizontal scaling matter.&nbsp;</span><span>But removing centralized state creates another problem.</span></p><p><b>JWT has Revocation Problem.&nbsp;</b><span>Suppose someone steals a valid JWT.&nbsp;With an opaque token stored in a database, the server can potentially delete or invalidate the corresponding session immediately.&nbsp;A purely self-contained JWT is different. If the token remains cryptographically valid and the application performs no additional revocation check, it can continue being accepted until it expires.&nbsp;Imagine issuing someone a building pass valid for 24 hours. If you revoke their access two hours later but every door only checks whether the pass itself is valid, the pass may continue working.&nbsp;This is why&nbsp;short-lived access tokens&nbsp;are important.&nbsp;But short-lived tokens create a usability problem.&nbsp;If an access token expires every 15 minutes, should users have to enter their password every 15 minutes?&nbsp;Usually, no.&nbsp;That leads to refresh tokens.</span></p><p><b>5. Access Tokens + Refresh Tokens</b></p><p>A common architecture uses&nbsp;two tokens with different jobs.&nbsp;Access Token is&nbsp;Short-lived and frequently presented to APIs.&nbsp;For example:&nbsp;Lifetime: ~15 minutes.&nbsp;Its job is fast authentication/authorization for normal requests.&nbsp;Refresh Token is&nbsp;Longer-lived and stored more carefully.&nbsp;For example:&nbsp;Lifetime: ~7 days&nbsp;Instead of accessing normal application resources, it is used to obtain a new access token.</p><p>The flow becomes:&nbsp;Login→&nbsp;Access Token + Refresh Token→Access Token → API requests→Access Token expires→Refresh Token → Authentication Server→New Access Token</p><p>The source describes a design where the access token is a JWT while the refresh token is opaque and server-stored, giving the system stateless access-token verification while retaining stronger revocation control over refresh sessions.This is an important general principle in authentication architecture:&nbsp;Security is often about combining mechanisms rather than finding one perfect mechanism.</p><p><b>6. Where You Store Tokens Matters</b><br></p><p>Authentication security doesn't end when the server creates a token.&nbsp;The client has to store it somewhere.The source warns about storing sensitive authentication tokens where arbitrary page JavaScript can read them because an XSS vulnerability could allow malicious JavaScript to steal them.&nbsp;For browser-based scenarios, it recommends appropriately configured&nbsp;HttpOnly cookies, which cannot be directly read by JavaScript, along with suitable SameSite protections.&nbsp;The broader lesson is simple:&nbsp;A secure token can become insecure if you store or transport it badly.</p><table><thead><tr><th>Situation</th><th>Potential approach</th></tr></thead><tbody><tr><td>Simple internal service</td><td>Basic Auth + HTTPS may be sufficient</td></tr><tr><td>Centralized web application</td><td>Session / opaque bearer token</td></tr><tr><td>Public API across multiple servers</td><td>JWT can reduce shared verification state</td></tr><tr><td>Distributed services</td><td>Signed JWTs can support decentralized verification</td></tr><tr><td>Need fast revocation</td><td>Server-side sessions / opaque tokens have advantages</td></tr><tr><td>Need scalability + revocation</td><td>&nbsp;Short-lived access tokens + refresh-token pattern<br></td></tr></tbody></table><p><span>In summary,&nbsp;&nbsp;the most useful web authentication principle is not “JWT is better” or “sessions are better.”&nbsp;It is:&nbsp;Match authentication complexity to your actual requirements.</span></p><div class=\"yt-embed\"><iframe src=\"https://www.youtube.com/embed/bP1mo3UbhNg\" title=\"YouTube video\" allow=\"accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share\" allowfullscreen=\"\"></iframe></div>",
  "contentType": "html",
  "autoRead": false,
  "pinned": false,
  "showHome": false,
  "parentId": "2026-09-07-web-authentication-fully-explained",
  "subpageSeq": 1
});
