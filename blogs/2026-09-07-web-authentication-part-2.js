window.BLOG_REGISTRY = window.BLOG_REGISTRY || [];
window.BLOG_REGISTRY.push({
  "id": "2026-09-07-web-authentication-oauth-20-tutorial-with-pkce",
  "title": "Web Authentication: OAuth 2.0 tutorial with PKCE",
  "author": "Sukee Parker",
  "date": "2026-09-07",
  "category": "Technology",
  "tags": [],
  "excerpt": "OAuth 2.0, a framework that lets third-party applications access a user’s data without requiring the user to give the application their password.",
  "content": "<p><strong>OAuth 2.0</strong><span>&nbsp;framework lets third-party applications access a user’s data</span><span>&nbsp;</span><strong>without requiring the user to give the application their password</strong><span>. It uses the analogy of a hotel key card: instead of giving someone a master key, OAuth gives an application a limited, temporary credential with specific permissions, called</span><span>&nbsp;</span><strong>scopes</strong><span>. The four main participants are the</span><span>&nbsp;</span><strong>resource owner (user), client application, authorization server, and resource server</strong><span>.</span><br></p><p>The most common approach is the&nbsp;<strong>Authorization Code Flow</strong>. When a user chooses something like “Sign in with Google,” the application redirects the user to Google. The user authenticates directly with Google and approves the requested permissions. Google then returns a short-lived&nbsp;<strong>authorization code</strong>, which the application's backend exchanges for an&nbsp;<strong>access token</strong>. That access token is then used to call Google's APIs, meaning the application never sees the user's Google password.</p><p>The transcript then introduces&nbsp;<strong>PKCE</strong>, pronounced “pixie,” as an additional security mechanism. Before authentication begins, the application creates a random&nbsp;<strong>code verifier</strong>&nbsp;and hashes it with SHA-256 to produce a&nbsp;<strong>code challenge</strong>. The challenge is sent during the authorization request, while the original verifier remains with the application. When exchanging the authorization code for a token, the application must provide the verifier. If an attacker steals only the authorization code, it is useless because they do not have the matching verifier.</p><p>OAuth also supports different&nbsp;<strong>grant types</strong>. Authorization Code is the normal user-facing flow;&nbsp;<strong>Client Credentials</strong>&nbsp;is intended for machine-to-machine communication such as microservices and backend jobs; and&nbsp;<strong>Device Authorization</strong>supports devices such as TVs, consoles, and command-line tools that cannot easily open a normal browser. The older&nbsp;<strong>Implicit Flow</strong>&nbsp;returns tokens directly through the browser URL and is described as deprecated because tokens can leak through browser history or logs. The recommended modern approach in the transcript is&nbsp;<strong>Authorization Code + PKCE</strong>.</p><p>OAuth commonly uses two types of tokens. An&nbsp;<strong>access token</strong>&nbsp;is short-lived and is used to access APIs. A&nbsp;<strong>refresh token</strong>lasts longer and allows the application to obtain new access tokens without forcing the user to sign in repeatedly. Tokens can also be revoked when access needs to be terminated.</p><p>The transcript closes with several security recommendations: avoid storing tokens in browser local storage because of XSS risks, use&nbsp;<strong>HTTP-only cookies</strong>&nbsp;where appropriate, use PKCE, require exact redirect URIs rather than wildcards, keep access tokens short-lived, use HTTPS, request only necessary scopes, follow least privilege, and rotate refresh tokens.</p><div class=\"yt-embed\"><strong>In one sentence:</strong><span style=\"caret-color: rgb(0, 0, 0); color: rgb(0, 0, 0); font-size: 0.88rem; font-family: var(--font);\">&nbsp;</span><span style=\"caret-color: rgb(0, 0, 0); color: rgb(0, 0, 0); font-size: 0.88rem; font-family: var(--font);\">OAuth 2.0 allows applications to obtain</span><span style=\"caret-color: rgb(0, 0, 0); color: rgb(0, 0, 0); font-size: 0.88rem; font-family: var(--font);\">&nbsp;</span><strong>limited, revocable access to another service on a user's behalf without sharing the user's password</strong><span style=\"caret-color: rgb(0, 0, 0); color: rgb(0, 0, 0); font-size: 0.88rem; font-family: var(--font);\">, while Authorization Code + PKCE protects the process of obtaining those access tokens.</span></div><div class=\"yt-embed\"><iframe src=\"https://www.youtube.com/embed/gEIfV3ZSt-8\" title=\"YouTube video\" allow=\"accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share\" allowfullscreen=\"\"></iframe></div>",
  "contentType": "html",
  "autoRead": false,
  "pinned": false,
  "showHome": false,
  "parentId": "2026-09-07-web-authentication-fully-explained",
  "subpageSeq": 2
});
