window.BLOG_REGISTRY = window.BLOG_REGISTRY || [];
window.BLOG_REGISTRY.push({
  "id": "2026-09-07-web-authentication-oauth-20-tutorial-with-pkce",
  "title": "Web Authentication: OAuth 2.0 tutorial with PKCE",
  "author": "Sukee Parker",
  "date": "2026-09-07",
  "category": "Technology",
  "tags": [],
  "excerpt": "OAuth 2.0, a framework that lets third-party applications access a user’s data without requiring the user to give the application their password.",
  "content": "<p>OAuth 2.0<span>&nbsp;framework lets third-party applications access a user’s data</span><span>&nbsp;</span>without requiring the user to give the application their password<span>. It uses the analogy of a hotel key card: instead of giving someone a master key, OAuth gives an application a limited, temporary credential with specific permissions, called</span><span>&nbsp;</span>scopes<span>. The four main participants are the</span><span>&nbsp;</span>resource owner (user), client application, authorization server, and resource server<span>.</span><br></p><p>The most common approach is the&nbsp;Authorization Code Flow. When a user chooses something like “Sign in with Google,” the application redirects the user to Google. The user authenticates directly with Google and approves the requested permissions. Google then returns a short-lived&nbsp;authorization code, which the application's backend exchanges for an&nbsp;access token. That access token is then used to call Google's APIs, meaning the application never sees the user's Google password.</p><p>The transcript then introduces&nbsp;PKCE, pronounced “pixie,” as an additional security mechanism. Before authentication begins, the application creates a random&nbsp;code verifier&nbsp;and hashes it with SHA-256 to produce a&nbsp;code challenge. The challenge is sent during the authorization request, while the original verifier remains with the application. When exchanging the authorization code for a token, the application must provide the verifier. If an attacker steals only the authorization code, it is useless because they do not have the matching verifier.</p><p>OAuth also supports different&nbsp;grant types. Authorization Code is the normal user-facing flow;&nbsp;Client Credentials&nbsp;is intended for machine-to-machine communication such as microservices and backend jobs; and&nbsp;Device Authorizationsupports devices such as TVs, consoles, and command-line tools that cannot easily open a normal browser. The older&nbsp;Implicit Flow&nbsp;returns tokens directly through the browser URL and is described as deprecated because tokens can leak through browser history or logs. The recommended modern approach in the transcript is&nbsp;Authorization Code + PKCE.</p><p>OAuth commonly uses two types of tokens. An&nbsp;access token&nbsp;is short-lived and is used to access APIs. A&nbsp;refresh tokenlasts longer and allows the application to obtain new access tokens without forcing the user to sign in repeatedly. Tokens can also be revoked when access needs to be terminated.</p><p>The transcript closes with several security recommendations: avoid storing tokens in browser local storage because of XSS risks, use&nbsp;HTTP-only cookies&nbsp;where appropriate, use PKCE, require exact redirect URIs rather than wildcards, keep access tokens short-lived, use HTTPS, request only necessary scopes, follow least privilege, and rotate refresh tokens.</p><p><strong>In one sentence:</strong><span>&nbsp;</span><span>OAuth 2.0 allows applications to obtain</span><span>&nbsp;</span>limited, revocable access to another service on a user's behalf without sharing the user's password<span>, while Authorization Code + PKCE protects the process of obtaining those access tokens.</span></p><div class=\"yt-embed\"><iframe src=\"https://www.youtube.com/embed/gEIfV3ZSt-8\" title=\"YouTube video\" allow=\"accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share\" allowfullscreen=\"\"></iframe></div>",
  "contentType": "html",
  "autoRead": false,
  "pinned": false,
  "showHome": false,
  "parentId": "2026-09-07-web-authentication-fully-explained",
  "subpageSeq": 2
});
